[project]
name = "frappe_crm_xt"
authors = [
    { name = "rtcamp", email = "support@rtcamp.com"}
]
description = "Frappe CRM extensions: search bar (frappe_search) and ERPNext-aware activity references."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[deploy.dependencies.apt]
packages = []
