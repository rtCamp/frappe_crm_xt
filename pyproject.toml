[project]
name = "frappe_crm_xt"
authors = [
    { name = "rtcamp", email = "support@rtcamp.com" }
]
description = "Frappe CRM XT: extensible sidebar list views, global search, and ERPNext bridge hooks."
requires-python = ">=3.10"
readme = "README.md"
license = { text = "AGPL-3.0" }
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"
crm = ">=1.74.0,<2.0.0"

[project.urls]
Repository    = "https://github.com/rtCamp/frappe-crm-xt"
"Bug Reports" = "https://github.com/rtCamp/frappe-crm-xt/issues"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017",   # assertRaises(Exception) - should be more specific
    "B018",   # useless expression, not assigned to anything
    "B023",   # function doesn't bind loop variable
    "E101",   # indentation contains mixed spaces and tabs
    "E402",   # module level import not at top of file
    "E501",   # line too long
    "E741",   # ambiguous variable name
    "F722",   # syntax error in forward type annotation
    "W191",   # indentation contains tabs
    "RUF001", # string contains ambiguous unicode character
    "UP030",  # Use implicit references for positional format fields (translations)
    "UP031",  # Use format specifiers instead of percent format
    "UP032",  # Use f-string instead of `format` call (translations)
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
